package pages;

import org.openqa.selenium.WebDriver;
import utils.MethodHandles;

public class SingUpPage extends MethodHandles {
    public SingUpPage(WebDriver driver) {
        super(driver);
    }
}
